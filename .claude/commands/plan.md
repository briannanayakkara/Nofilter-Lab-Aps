---
description: Produce an implementation plan for a feature or fix in this repo before writing code
argument-hint: <description of the feature or fix>
---

Plan the following change to the Nofilter Lab app: $ARGUMENTS

Use the `planner` subagent. Read `memory/PRD.md` first for current architecture and the deferred/backlog list. Read the actual current code for anything the plan touches — don't plan against a remembered version of `server.py` or the frontend.

Output a numbered step list with concrete file paths. Call out any open decisions that need my input before implementation starts.
