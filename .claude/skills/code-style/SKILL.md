---
name: code-style
description: Code style conventions for this repo (FastAPI backend, React frontend). Use before writing or editing backend/server.py or frontend/src code so new code matches existing patterns.
---

Read `references/style-guide.md` for the conventions actually used in this codebase (derived from the existing code, not a generic style guide). Follow it when writing or editing:
- `backend/server.py` and anything under `backend/`
- anything under `frontend/src/`

If you're about to write code that doesn't fit a documented pattern, match the nearest existing analogous code instead of introducing a new convention.
